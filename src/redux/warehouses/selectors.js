export const selectWarehouses = (state) => state.warehouses.warehouses;
export const selectCityQuery = (state) => state.warehouses.cityQuery;
export const selectCurrentPage = (state) => state.warehouses.currentPage;
export const selectIsLoading = (state) => state.warehouses.isLoading;
export const selectError = (state) => state.warehouses.error;
